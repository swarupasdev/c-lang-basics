# c-lang-basics
All related to c

### varibale and constant 
- variable is like a container which can hold a particular type of value, which type is defined by the datatype
- holds a single value at a time
- eg: ```int x;``` this is initialised by garbage values
- x is variable , int is datatype
- when variable is defined memory is allocated for it(2 bytes or 4 bytes for integer which varries compiler to compiler)
- value that was present in those 2 byte is known as garbage value.
- eg: ```x = 5;```
- the value which is assigned to the variable for the 1st time is called initialization
- declaration and define is 